"use client";
import {useEffect,useState} from "react"; import AppShell from "@/components/AppShell"; import {loadItems,Item} from "@/components/Data";
export default function History(){const [items,setItems]=useState<Item[]>([]);const [msg,setMsg]=useState("");useEffect(()=>{const r=async()=>{try{setItems(await loadItems());setMsg("")}catch(e:any){setMsg(e.message)}};r();window.addEventListener("zorix-data",r);return()=>window.removeEventListener("zorix-data",r)},[]);return <AppShell><main className="container"><h1 className="title">History</h1><p className="sub">Activity for your signed-in account.</p>{msg&&<div className="warning">{msg}</div>}<section className="panel">{items.length===0?<p className="muted">No activity.</p>:<div className="tableWrap"><table className="table"><thead><tr><th>Type</th><th>Plan</th><th>Amount</th><th>Reference</th><th>Status</th><th>Date</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{x.type}</td><td>{x.plan||"-"}</td><td>{x.amount} USDT</td><td>{x.tx.length>24?`${x.tx.slice(0,24)}...`:x.tx}</td><td><span className="status">{x.status}</span></td><td>{new Date(x.date).toLocaleString()}</td></tr>)}</tbody></table></div>}</section></main></AppShell>}
