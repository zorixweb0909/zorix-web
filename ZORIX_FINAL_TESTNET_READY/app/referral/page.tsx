"use client"; import {useEffect,useState} from "react"; import AppShell from "@/components/AppShell";
export default function Referral(){const [code,setCode]=useState("");const [link,setLink]=useState("");const [message,setMessage]=useState("");useEffect(()=>{const saved=localStorage.getItem("zorix_ref_code");const next=saved||`ZORIX-${Math.random().toString(36).slice(2,8).toUpperCase()}`;localStorage.setItem("zorix_ref_code",next);setCode(next);setLink(`${location.origin}/?ref=${next}`)},[]);const copy=async()=>{if(link)await navigator.clipboard.writeText(link);setMessage("Referral link copied.")};return <AppShell><main className="container"><p className="eyebrow">GROW YOUR NETWORK</p><h1 className="title">Referral Center</h1><p className="sub">Your browser profile receives a unique code and shareable link for testing the referral journey.</p><div className="twoCol"><section className="panel"><h2>Your referral code</h2><div className="refCode">{code||"Loading..."}</div><p className="muted">Share this code with people who want to test the platform flow.</p></section><section className="panel"><h2>Your referral link</h2><div className="addressBox">{link||"Loading..."}</div><button className="button topGap" onClick={copy}>Copy Referral Link</button>{message&&<p className="success">{message}</p>}</section></div><section className="panel"><h2>Referral status</h2><div className="miniStats"><div><span>Total referrals</span><b>0</b></div><div><span>Active referrals</span><b>0</b></div><div><span>Referral rewards</span><b>Not configured</b></div></div><p className="small">A real referral reward program needs server-side identity, anti-fraud controls, clear terms, and a verified reward source.</p></section></main></AppShell>}
